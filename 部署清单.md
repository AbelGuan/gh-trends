# 部署清单：GitHub Actions + 飞书推送

> 前置：你已经有一个 GitHub 账号。全程 **不用装任何东西**，本机只负责把代码推上去一次。
> 下面标 ⚠️ 的地方我无法替你操作（需要你的账号/网页点击）。

## 第 0 步：在 GitHub 建一个空仓库

⚠️ 网页操作：<https://github.com/new>

- Repository name：`gh-trends`（随意，后面 URL 要对应）
- 选 **Public**（公开仓库用 Actions 不计分钟，也不受私有仓库 2000 分钟额度限制）
- **不要**勾 "Add a README file" / .gitignore / license（要一个空仓库，否则首次 push 会冲突）

## 第 1 步：把本地代码推上去

在本机执行（把 `<你的用户名>` 换成你的 GitHub 用户名）：

```bash
cd /d/Pi/Chat/gh-trends
git init -b main
git add -A
git commit -m "feat: GitHub Trending 每日抓取 + 飞书推送"
git remote add origin https://github.com/<你的用户名>/gh-trends.git
git push -u origin main
```

首次 push 会弹浏览器让你登录 GitHub（Git Credential Manager 的流程），授权一次即可。

> 如果 `git init` 报"already a repository"，说明这个目录里已经有 `.git`，那前两条跳过。

## 第 2 步：飞书建群机器人

⚠️ 在飞书里操作：

1. 建一个群（或者用现有的、只有你自己的群）
2. 群设置 → **群机器人** → **添加机器人** → **自定义机器人**（Custom Bot）
3. 起个名字，比如 `GitHub 趋势`
4. **安全设置**二选一：
   - **签名校验**（推荐）：勾上后会给你一个 `Secret`，记下来
   - **自定义关键词**：填一个词，比如 `GitHub`，机器人只会转发包含这个词的消息（我们的卡片标题里就带 "GitHub"）
5. 复制 **Webhook 地址**，形如
   `https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`

> 注意：webhook 地址等同于一把钥匙，**不要提交到仓库、不要发到公开地方**。放 GitHub Secrets 就是为此。

## 第 3 步：把凭据存成 GitHub Secret

⚠️ 网页操作：仓库 → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**

| Name | Value |
| --- | --- |
| `FEISHU_WEBHOOK` | 第 2 步复制的 webhook 地址 |
| `FEISHU_SECRET` | 第 2 步的 Secret（**只勾了「自定义关键词」就不要建这个**） |

## 第 4 步：手动跑一次验证

⚠️ 网页操作：仓库 → **Actions** → 左侧选 `daily-trends` → 右侧 **Run workflow** → 绿色按钮

跑完应该看到：
- 群里收到一张卡片（标题 `GitHub Trending 今日榜 · 2026-xx-xx`，前 10 名）
- 仓库里多出 `reports/` 和 `data/` 的提交

如果失败：
- 红叉日志里能看到具体报错；飞书推送那一步如果打印 `❌ 飞书返回异常`，把返回内容拿来我看
- 想不碰飞书先测流程 → 把 `FEISHU_WEBHOOK` 删掉，脚本会跳过推送并打印内容，流程不会失败

## 第 5 步（可选但推荐）：开 GitHub Pages

开了之后卡片里的「查看完整看板」按钮才有地方点，也是 Obsidian 订阅的前提。

⚠️ 网页操作：仓库 → **Settings** → **Pages**

- **Source**：`Deploy from a branch`
- **Branch**：`main` + **/ (root)** → **Save**

过 1~2 分钟访问 `https://<你的用户名>.github.io/gh-trends/`，会自动跳到趋势看板。
这时卡片里的按钮也会自动指向正确的地址（workflow 用 `${{ github.repository_owner }}` 拼的）。

## 第 6 步：确认定时

`daily.yml` 里的写法是：

```yaml
on:
  schedule:
    - cron: "10 1 * * *"    # UTC 01:10 = 北京时间 09:10
```

- **cron 是 UTC**，要改成别的时间记得减 8 小时
- 避开整点（Actions 和飞书在整点都容易拥堵延迟）
- 改完 commit 上去就生效

## 一个可能的坑：60 天规则

GitHub 官方文档写明：

> In a public repository, scheduled workflows are automatically disabled when no repository activity has occurred in 60 days.

我们的 workflow **每天都会往仓库提交报告**，所以理论上会持续产生活动、不会被禁用。
但「机器人用 `GITHUB_TOKEN` 提交算不算"活动"」这点我没有找到权威说明 —— **不确定**。

保险做法（不花成本）：偶尔自己去仓库点一下、改个 README，或者留意 GitHub 发来的 "workflow disabled" 邮件。
如果真被禁了，Actions 页面点一下 Enable 就恢复。

## 本地手动跑（调试用）

```bash
cd /d/Pi/Chat/gh-trends
python gh_trends.py                     # 抓一次，出报告
python notify_feishu.py --dry-run       # 只看将要发给飞书的内容
python notify_feishu.py --webhook "https://open.feishu.cn/open-apis/bot/v2/hook/xxx"
python notify_feishu.py --text          # 卡片格式出问题时用纯文本兜底
```

`FEISHU_WEBHOOK` / `FEISHU_SECRET` / `PAGES_BASE` 都可以用环境变量传，避免写进命令历史。
